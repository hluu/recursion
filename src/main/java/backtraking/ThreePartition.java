package backtraking;

/**
 * Given a set X of 3n positive integers, can X be partitioned into n three-element subsets,
 * all with the same sum.
 *
 * Let say n is 5, then set X consists of 15 positive integers.
 * Can X be partitioned into 5 3-element subsets.
 * Each subset sum must be == sum(X) / 5
 *
 */
public class ThreePartition {
    public static void main(String[] args) {
        System.out.println("ThreePartition.main");
    }
}
